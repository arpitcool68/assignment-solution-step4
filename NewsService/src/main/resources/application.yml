spring:
 data:
   mongodb:
     database: news
     port: 27017
     host: localhost
server:
 port: 8081
 servlet:
    context-path: /newsService
 
logging:
  level:
    org:
      mongodb:
        driver: ERROR